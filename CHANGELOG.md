# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.5.2] — 2026-09-23

公开页补上两张信息：每个账号**今天用了多少 token**，以及**这份数据是什么时候刷新的**。

### 新增

- **账号池下拉每条显示「今日已用 N tokens」**（投影新增 `tokens_today` 字段）。
  「今日」按**日期**判，不照抄网关的 `tokensToday`：网关的跨日重置是**惰性**的
  （`RecordUsage` 遇到新日期才清零），昨天用过、今天还没被派活的账号仍挂着昨天的数，
  直接透传会把昨天的量报成今天。面板侧比对 `tokensDate` 与当天日期，不符即按 0 出 ——
  与网关自己的 `CostToday` 同一口径。日期字段缺失时同样往 0 倒，不往「照抄」倒。
- **页面顶部显示「数据刷新于 HH:MM:SS · 每 30 秒自动刷新」**。页面本来就在轮询，
  但读者无从判断眼前这份数字有多新；刷新失败时改红字并保留最近一次成功的时间
  （「刷新失败，正在重试 · 最近一次成功 12:03:41」）。

### 说明

- 公开面只给**今日**用量，**累计**（`tokensTotal`）仍是运营口径，不进公开面；
  白名单没变宽 —— 键集合断言同步更新，哨兵扫描新增「累计量不得出现」。
- 账号行排版跟着调整：状态标签改为与详情一起靠右（`.acct .tag{margin-left:auto}`），
  邮箱与今日用量留在左侧成一组。

### 测试

- 新增 4 项：今日 token 只认当天日期（昨天的残留归零）、日期字段缺失时不冒领、
  渲染所需的数值类型、页面含刷新时间标记；stub 里加了一条 `tokensDate` 是 2020 年的
  账号（`tokensToday=9999`），冒烟与 Caddy 验证脚本据此断言「昨天的量被归零」。
  面板单测 185 项、Caddy 路由验证 58 项全通过。

## [0.5.1] — 2026-09-23

公开页新增**账号池下拉**：可展开查看池内全部账号，邮箱只显示打码后的前 3 个字符，
带状态（可用 / 冷却中 / 已停用）与冷却、限额提示 —— 贡献者能认出自己那个账号。

### 安全

- **打码在面板侧独立再做一遍**，不依赖网关的 `MaskEmail`：后者在 local part 不足
  3 字符时会**原样返回完整地址**（实测 `ab@x.com`、`a@b.co` 都漏），那是管理员口径。
  新函数 `_public_email` 对任何形状都保证出现 `***`，并保持幂等（网关已打码的不重复打）。
- 公开面的账号条目**不含 accountId**（公开面无法对它做任何操作，少一个标识少一分
  枚举面），也不含请求数/token 量（运营口径）。

### 测试

- 新增 8 项：打码函数（3 字符上限、幂等、任何形状都不漏完整地址、异常输入）、
  账号投影的键白名单、稳定排序（能出力的在前）、状态与限额字段；
  stub 里加了一条**网关没打码成功的短邮箱**，冒烟测试据此断言面板补上了这一层。

## [0.5.0] — 2026-09-23

新增 **Cline 公开面**（`/cline/`，免登录）：任何人可查看 Cline 免费额度池的状态，
也能用自己浏览器里已登录的 Cline 账号**贡献到池子里**。免费额度按账号摊薄，
多一个账号大家一起多一份额度。

**边界是这个版本的重点** —— 公开面开放了「贡献」，但**不得**因此获得「停用」的能力：

### 新增

- **公开页 `public.html`**（`/cline/`）—— 只读快照（可用账号数、池内账号数、
  逐模型的可用状态）＋「开始贡献」按钮。贡献走 Cline 官方设备授权，成功后自动入池。
- **只读投影 `_public_status` / `_public_model`**（panel.py）—— 对外字段是**白名单**：
  逐个具名挑选，不是「取回来再删几个」。网关新增字段**不会**自动流到公开面。
  公开面不含账号邮箱与 id、上游模型名、闸门台账细节、网关地址与密钥。
- **贡献代理**（`ClinePanel.public_contribute_*`）—— 转发到网关的 `/public/login/*`。
  网关版本较旧时回 501 并给出可读提示；网关限流时回 429 并带上等待秒数。
- **公开面与管理面是两条独立通路**：`/cline/api/*` 与 `/api/cline/*` 各自实现，
  管理面一行未改。公开面也没有任何启停入口 —— 那些路径在公开前缀下不存在（404），
  不是被拒绝。

### 设计要点

- **公开面的 POST 不过 CSRF 闸门**：它唯一的写操作是「发起一次账号捐赠」，任何人都能
  发起、也随时能取消，借他人浏览器提交得到的只是「这个人自己发起了一次捐赠」。
  而要求自定义头会因跨站预检被挡，等于把公开页上的贡献按钮废掉。管理员面 `/api/*`
  的闸门一个字没动。
- **未配置 cline2api 的部署**：公开面回 503 并说明原因（与既有降级形态一致）。

### 测试

- 新增 15 项：投影白名单（喂一份塞满敏感字段的响应，断言输出键集合**恰好**等于预期）、
  哨兵值不泄漏、状态词表翻译、空/残缺负载不崩、等待秒数解析；公开面端到端
  （免凭据可读、贡献链路、免 CSRF 头、**写操作全 404**，以及「公开面的写请求没有
  转发到网关」的反证）。
- Caddy 侧新增 12 项断言：`/cline → 301`、免认证可达、贡献链路、
  公开面够不到管理写操作、`/cline/*` 不是 `/panel/*` 的后门。

## [0.4.1] — 2026-09-21

跟随 cline2api 的**模型级限额**口径（免费额度按账号×模型计）：账号卡片显示它当前
被哪些模型限额、到什么时候，并给出「清除冷却」按钮。

### 新增

- **账号卡片列出限额模型** —— 网关 `/status` 的账号对象新增 `modelCooldowns`
  （`[{model, until, reason}]`）。面板逐条显示「限额模型 / 到期时间」并标注
  「其他模型不受影响」，与账号级「冷却中」是卡片上两个不同的标记。
- **「清除冷却」按钮** —— 转发到网关 `POST /admin/accounts/{id}/clear-cooldown`，
  一次清掉账号级冷却与全部模型级限额。额度提前重置、或升级完要立刻恢复服务时用。
- 新增 4 项冒烟测试（清除冷却的转发与缺参、限额字段透传、stub 回执）。

## [0.4.0] — 2026-09-21

新增**任务中心**：一键完成官方成长任务。吸收自
[linguo2625469/workbuddy2api-panel](https://github.com/linguo2625469/workbuddy2api-panel)
的目标能力，但落在面板侧编排、脚本在网关侧执行，不 fork 网关。

### 新增

- **任务中心卡片**（wb2a 页内，与「模型目录」「请求统计」同一套折叠机制）——
  四个按钮：扫描待办（只读）／一键完成全部／只领奖／开学季盘点，运行中可取消。
- **后台作业模型** —— 任务运行是分钟级的（单账号含真实对话任务 1–4 分钟），
  不能沿用 `run_tool()` 的同步阻塞（HTTP 挂 150 秒会被浏览器/反代/systemd 任一环断开）。
  改为后台线程跑进程、HTTP 只做启动与轮询，与登录会话同一套模式。
- **日志解析器** —— 脚本没有结构化输出，进度从日志行解析（`[task_runner]` /
  `[school2026]` 前缀、汇总行 k=v、奖励 credit=/energy=）。**认不出的行降级为原始
  日志，不生成假的进度条目** —— 脚本是上游在活跃维护的，文案会变，显示「未知」
  比显示错误的状态安全。
- **逐项状态表** —— 按账号分组，每项显示状态（✅已完成 / ✓已领 / ◐进行中 /
  ◇待执行 / ○待下次 / —跳过 / ✗失败）、进度（x/y）、奖励与脚本原文。
- **增量日志轮询** —— `/api/tasks/status?since=N` 只回新行，2 秒轮询不重传全量；
  日志环形封顶 2000 行，长作业不吃光内存。
- **flock 互斥** —— 面板内重复点击返回 409 并提示当前作业；`tasks.lock` 挡同机
  第二个面板实例。**挡不住网关自己的排程**（脚本不带跨进程锁），已在 README 说明。
- **能力探测**（`/api/endpoint` 的 `tasks` 段）—— 脚本在位 + 能读凭据 + 有解释器
  三者齐备才启用；缺失时禁用按钮并给出替代命令，与「新增账号」同构。

### 实现要点

- 子进程环境强制 `PYTHONUNBUFFERED=1`：不设的话脚本照常跑完，但面板在整个运行
  期间**一行输出都收不到**，界面看起来像卡死。另设 `PYTHONIOENCODING=utf-8`
  兜住服务器 `LANG=C`。
- 输出按 bytes 读、`errors='replace'` 解码，与既有 `run_tool()` 同口径。
- 账号入参用**完整文件名**（`workbuddy-<uid>.json`）而非 uid 前缀 ——
  前缀会撞号（`abc123` 命中 `abc1234` 的凭据）。
- 作业收尾顺序：先释放锁与回调、再落终态，使「running=False」蕴含「锁已释放」，
  调用方看到结束即可安全启动下一个。

### 测试

新增 `tests/test_tasks.py`（解析器各状态文案、未知行降级、环形封顶、命令构造、
写操作闸门、能力降级、进程执行与取消、flock 互斥、子进程环境）与
`tests/test_smoke.py::TestTaskSmoke`（端到端：真起进程走 HTTP 通路，含「扫描不带
`--yes`」的只读断言与 CSRF 闸门）。另有 `tests/stub_tasks.py` 假脚本，按真实文案
输出但不发网络请求。合计 **148 项全绿**。

### 修复

- **`account_op` 的 `enable` 分支 `UnboundLocalError`** —— 重构时删掉了
  `payload = None` 初始化，`enable` 走到 `http()` 就崩（HTTP 侧表现为连接被断）。
  已加回并注明原因。
- **冒烟测试不再受开发机影响** —— `TestSmoke` 此前会向上探测到本机真实的
  `wb2a/app`（带 linux 二进制），能力探测结果随机器而变。现显式指向不存在的
  网关配置，锁定「无 CLI 工具时的降级形态」。

## [0.3.2] — 2026-09-20

闸门台账的口径从「成本（美元）」改为「扣费（积分）」，与网关新的判定源对齐。

### 变更

- **台账字段改名** — 「最近成本 / 累计成本」→「最近扣费 / 累计扣费」，并带上「积分」
  单位。网关的判定源已改为 Cline 后台积分台账的 `creditsUsed`（用户真实被扣的积分），
  面板继续写「成本」会让人按旧口径理解闸门。
- **闸门说明改写** — 明确写出「按 generation id 回查后台积分台账，确认真实扣了积分才
  关停」，并补一句「响应里的 cost 字段是市场参考价，免费模型也非零，不作为判定依据」。
  这句是给运维看的：它正是此前误关停三个免费模型的原因。
- **手动启用提示改写** — 「启用后先经探针，cost=0 才真正放行」→「启用后按积分台账核对，
  确认为免费才放行」（探针本身也改成读台账了）。

### 测试

- 新增 2 项冒烟：台账文案必须是「扣费/积分」且不得残留「成本」（`test_cline_ledger_shows_credits_not_market_price`），
  字段值语义须是积分（`test_cline_ledger_values_are_credits`）。72 项全绿。

## [0.3.1] — 2026-09-20

管理界面按网关拆页：两个上游网关各自独立一页，不再上下堆叠。

### 变更

- **标签页分离** — 顶部加「workbuddy2api / cline2api」两个标签，一次只看一个网关。
  此前两个网关的内容在同一页上下铺开，滚到中段才分得清「这块是谁的」；两个标签
  还各带一枚徽标（账号数 / 在服模型数），不切页也能看出对面是否正常。
- **cline 页内分区** — 账号池与定价闸门台账各自成区、各有标题与计数。此前两类卡片
  混在同一个网格里（一个管凭据额度、一个管计费闸门），语义完全不同却并排出现，
  看起来像数据错乱。
- **cline 页自己的接入信息** — Base URL / API Key + 一句「只绑回环、由 new-api 经
  priority=102 接入」的说明，不必再去翻配置文件。
- **副标题跟着当前页走** — 在 cline 页上显示的是 cline2api 的地址。此前恒显 wb2a 的
  「账号池 · 模型目录 · 请求统计 · 网关 http://127.0.0.1:7863」，在 cline 页上是错的。
- **轮询只刷当前页** — 两个网关互不干扰，也不再每 15 秒各打一次接口。

### 修复

- **登录弹窗取消打错网关** — 弹窗被两个网关共用，取消时却固定打 `/login/cancel`；
  在 cline 的登录流程里点取消会去取消 wb2a 的会话（对面没有该会话，等于没取消）。
  现按发起方路由到 `/login/cancel` 或 `/cline/login/cancel?device_code=...`。
- **读不到 cline2api 配置时不再静默降级** — 「文件在但权限不足」与「没部署这个网关」
  此前表现完全一样（整块区域消失）。现在启动时明确报错，并给出可直接执行的修复命令。
  背景：cline2api 的部署脚本会把 config.json 设成 600，面板（wb2a 用户）读不到 ——
  这个组合真实踩过一次，排查成本很高。

### 工程

- 测试：62 → 70 个（新增 cline 配置发现 5 项、标签页与分区结构 3 项）

## [0.3.0] — 2026-09-20

支持第二个上游网关（cline2api）：面板可以同时管理 workbuddy2api 与 Cline 账号池。

### 新增

- **cline2api 子面板** — 配置后同页渲染「Cline 免费层」一节：
  账号池（状态/请求/token/冷却原因、临时停用）、模型定价闸门台账
  （分组、状态、最近与累计计费成本、关停原因、手动启停）、设备授权登录代理。
  配置走四级发现（命令行 > 环境变量 > `panel.json` > cline2api 的 `config.json`），
  通常 `--cline-config /opt/cline2api/config.json` 一条就够。
- **测试用 cline2api stub**（`tests/stub_cline.py`）—— 按真实形状提供 `/status`、
  `/v1/models`、`/admin/login/*`、`/admin/models|accounts/{id}/{enable,disable}`，
  并按真实网关分两套鉴权（admin_token vs api_key）。

### 说明

- **未配置 cline2api 时行为不变**：`/api/cline/*` 返回 503，页面上那一节不出现。
  冒烟测试专门覆盖了这一降级形态。
- cline2api 的上游模型名含 `/` 与 `:`，转成管理端点路径时必须 URL 转义
  （Go 1.22 ServeMux 的 `{id}` 只吃一段）；冒烟测试断言转义-解转义后取回原值。
- 面板未配置 cline2api 时不再每 15 秒轮询一次该接口（避免日志刷 503）。

### 工程

- 测试：51 → 62 个（单元 36 不变，冒烟 15 → 26）

## [0.2.0] — 2026-09-19

支持挂在反向代理的子路径下，以及为此必需的防护措施。

### 新增

- **子路径挂载** — 页面里的 API 基址改为从 `location.pathname` 推导，
  可挂在 `https://example.com/panel/` 这类路径下（代理负责剥前缀）。
  直接跑在根路径的行为不变。
- **安全响应头** — `X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、
  `Referrer-Policy: no-referrer`

### 安全

- **CSRF 闸门** — 状态变更接口（POST）现在要求带 `X-Panel-Request: 1` 头。
  背景：面板常被放在带认证的反代后面，而认证凭据是浏览器自动附带的，
  跨站页面也能触发 POST 且不需要预检；自定义头会强制预检，被 CORS 挡下。
  浏览器内正常点击不受影响；脚本调用需显式加该头。**这是一处破坏性变更**，
  升级后调用面板 API 的脚本要跟着改。

### 工程

- 测试：48 → 51 个（新增 CSRF 拒绝、安全响应头、API 基址为相对路径三项）
- 冒烟测试的 `_post` 辅助函数默认带上 CSRF 头，并新增 `csrf=False` 以验证拒绝路径
- 修正 README 里过时的测试计数（原文写 31，实际 48）

## [0.1.0] — 2026-09-18

首个版本。

### 新增

- **账号池总览** — 账号数 / CN·Global 分布 / 健康数 / 异常数
- **账号卡片** — 区域标签、积分进度条、成功次数、连续失败、冷却 / 熔断 / 降级状态
- **双位状态展示** — 区分「手动停用」与「自动禁用」，叠加态分别标注
- **模型限流台账** — 逐账号列出被限流的模型与恢复时间
- **模型目录** — 按 `cn:` / `global:` 分列，档位模型（快速/均衡/极致）高亮，
  点名字复制的字符串自带区域前缀
- **请求统计** — 按模型聚合请求数、成功率、平均延迟、tokens、缓存命中率、计费
- **下游接入信息** — Base URL + API Key 一键复制，附可运行 curl 示例
- **临时停用 / 恢复** — 走网关管理端点；未开启时给出开启指引
- **新增账号** — CN / Global 双入口，只展示登录链接、不自动打开窗口
- **实时积分 / CN 签到 / 领 Global 试用** — 调用网关自带 CLI

### 工程

- 零依赖：仅 Python 标准库（3.8+），无需 pip / 编译 / 构建
- 配置发现：命令行 > 环境变量 > 面板配置 > 自动探测网关 `config.json`
- 能力探测：检测不到网关 CLI 时禁用对应按钮并说明原因与替代方案
- 测试：36 个单元测试 + 一个可复用的 stub 网关（`tests/stub_gateway.py`）
- CI：Python 3.8 / 3.10 / 3.12 / 3.13 矩阵 + 真实启动冒烟测试
- Docker：alpine 基础镜像，非 root 运行，带 healthcheck

[0.2.0]: https://github.com/wellwei/wb2a-panel/releases/tag/v0.2.0
[0.1.0]: https://github.com/wellwei/wb2a-panel/releases/tag/v0.1.0
