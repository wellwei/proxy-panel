# 设计实现文档 — 任务中心（一键完成成长任务）

> 吸收对象：[linguo2625469/workbuddy2api-panel](https://github.com/linguo2625469/workbuddy2api-panel)
> 的目标能力（一键完成官方成长任务 + 任务中心编排），落在**本面板**而不是 fork 网关。
> 版本：面板 0.3.2 → 0.4.0。

## 一、结论先行：为什么是「面板编排 + 脚本执行」

上游那个仓库**不是面板**，是把网关整个 fork 之后把面板和任务引擎嵌进同一个 Go 进程
（`internal/panel/`，7 个视图 34 个端点）。它最有价值的资产不是界面，而是**那套「怎么用纯
API 完成官方成长任务」的知识**：18 个官方任务里 17 个可纯 API 完成，靠四套客户端指纹
（CLI / 桌面 / Web / 小程序）伪装上报，再轮询回读、自动领奖。

关键判断：**这套知识我们已经在网关层吸收过了。**

`cliproxy/wb2a/src/scripts/task_runner.py`（1451 行，作者 Sliverkiss，即本网关上游作者本人，
2026-09-16/17 提交）文件头注释即写明「参考实现：linguo2625469/workbuddy2api-panel
（autotask.go / desktop.go / report.go / tasks.go 三账号实测）」。它的 MAPPING 表列了
**23 个可执行 task_code + 1 个标注不可伪造**（`Expert_Philanthropy` 需真实捐款），
比上游 registry 的 19 条还全：

| 脚本 | 覆盖 | CLI |
|---|---|---|
| `scripts/task_runner.py` | growth 域 17 个 + mp 成长 2 个 + school 4 个 | `<uid前缀\|ALL> [--yes] [--only CODE] [--only-claim] [--gap N]` |
| `scripts/school_open_day_2026.py` | 开学季小程序任务 + 转盘抽奖 | `<uid前缀\|ALL> [--list\|--run\|--lottery-only] [--yes]` |
| `scripts/task_common.py` | 纯 HTTP 工具层（auth 加载、端点常量、上报构造） | 库，无 CLI |

所以缺的**不是任务能力，而是面板侧的一键入口与进度可视化**。这部分是纯 Python 面板工作，
符合本面板「不 fork 网关、零依赖、优雅降级」的三条既有取舍，不需要改一行 Go。

### 被否决的方案

| 方案 | 否决理由 |
|---|---|
| 移植上游 `autotask.go` / `taskcenter.go`（约 2500 行 Go）进网关，暴露 `/v1/tasks/*` | 要背 2500 行代码并与活跃的 Sliverkiss 上游持续同步；而脚本路线**正是上游作者自己在维护的**。等他自己加 HTTP 端点，那时是免费的。 |
| 面板直接读 `auths/*.json` 自己发上报 | 越过网关职责边界，把凭据副本散到第二个进程；且四套指纹的常量、轮询判据、领奖降级路径都要在 Python 里重抄一遍。 |
| 面板走 `/v1/chat/completions` 让网关代发上报 | 网关没有这类端点，等于方案一的变形，还多一层协议设计。 |

## 二、既有通路已验证：这不是新架构

面板现有的增强功能（签到 / 积分 / 试用）就是**shell 调用网关自带 CLI**：

```
面板 ──subprocess──▶ /opt/wb2a/app/signin_bin ──读 ./auths/──▶ 腾讯上游
```

而这些 CLI **自己读 `auths/`**，凭据既不经过面板、也不进面板的进程内存。让面板再调一个
`task_runner.py` 是**完全同一条既有通路**，不是新架构。三处细节佐证可行性：

1. `task_common.py::_resolve_auths_dir()` 用 `__file__` 自定位（`scripts/../auths`），在服务器上
   正好解析到 `/opt/wb2a/app/auths` —— 与网关 `auth_dir: ./auths` 指向同一目录，**零配置**。
2. 云端部署包已经带着 `scripts/`（`deploy-cloud.sh` rsync 整个 `app/`），服务器也有 python3
   （部署报告写明它是「wb2api 排程脚本」的依赖）。
3. panel 的 systemd 单元（`deploy/wb2a-panel.service`）以 `User=wb2a` 运行、`ReadWritePaths=/opt/wb2a`、
   `PrivateTmp=true`。`/opt/wb2a/app/auths` 对 wb2a 可读，python3 在 `/usr` 只读可执行 ——
   **加固策略不影响拉起子进程**。

## 三、架构

```
浏览器 ──▶ 面板 panel.py ──Popen(流式 stdout)──▶ python3 scripts/task_runner.py ALL --yes
              │   ▲                                        │
              │   └── 解析日志行 → 逐项状态 ────────────────┘
              └── 后台作业 + 环形缓冲，HTTP 层只做轮询
```

### 3.1 后台作业模型（为什么不能用现有 `run_tool`）

现有 `run_tool()` 是**同步阻塞**的：`subprocess.run(..., timeout=150)`，HTTP 请求一直挂到结束。
这对 `signin_bin`（秒级）合适，对任务运行**不行**：

- 单账号全量含真实对话任务（专家召唤 5 次，每次间隔 6 秒），上游实测量级 **1–4 分钟/账号**；
- 全账号运行是分钟到十几分钟级；
- HTTP 层挂这么久，浏览器、反代、systemd 都可能提前断开。

所以引入**后台作业 + 轮询**，与现有登录会话（`session` + `_login_worker` 线程）**同一套模式**：

```
线程：Popen 读 stdout 逐行 → 写环形缓冲 → 解析进 items → 进程退出后落 summary/exit_code
HTTP：POST 启动（立刻返回 job id） / GET 轮询状态与增量日志 / POST 取消
```

### 3.2 并发约束

- **面板内**：同一时刻只允许一个任务作业（`_task_busy`），重复点击返回 409 并附当前作业信息。
  理由与上游一致：任务运行有真实上游副作用，重复触发会白发上报、抢领奖。
- **跨进程**：日志脚本与网关排程可能撞车 —— 网关 config 里 `school_hours: [12]`、
  `cat_hours: [1]` 会由 Go scheduler 拉起同一批脚本。面板侧做两件事：
  1. 作业持一把 `flock` 咨询锁（`<面板目录>/tasks.lock`），面板自身的作业互斥；
  2. 启动时若落在排程窗口附近，**提示**而非阻止（跨进程锁需要改网关，见第七节）。

## 四、接口设计

### 4.1 面板端点（新增）

| 方法 | 路径 | 语义 | 写操作 |
|---|---|---|---|
| GET | `/api/tasks/status?since=N` | 作业状态 + 增量日志（seq > N 的 `lines`）。`since` **可选**：不传不带日志，传 0 表示要全量 —— 用「缺省」而非 0 判定，否则前端首次轮询会一行都收不到 | — |
| POST | `/api/tasks/scan` | **扫描待办**（dry-run，`task_runner.py ALL`） | 否 |
| POST | `/api/tasks/run` | **一键完成**（`--yes`）；body `{accounts, only_claim, only, gap}` | 是 |
| POST | `/api/tasks/school` | 开学季：body `{mode: list\|run\|lottery}` | run/lottery 是 |
| POST | `/api/tasks/cancel` | 终止当前作业（SIGTERM，超时后 SIGKILL）；无作业时 409 | 是 |

> 实现时把「日志」合进了 status 的 `since` 参数（原设计的独立 `/api/tasks/log` 端点撤销）：
> 两者永远一起被读，合成一个请求少一次往返，前端每 2 秒只打一个接口。

CSRF 闸门沿用 `do_POST` 顶部那把（要求 `X-Panel-Request: 1`），新端点自动覆盖，无需另写。

### 4.2 参数映射

| 面板动作 | 实际命令 |
|---|---|
| 扫描待办 | `python3 scripts/task_runner.py ALL` |
| 一键完成（全部账号） | `python3 scripts/task_runner.py ALL --yes` |
| 一键完成（指定账号） | `python3 scripts/task_runner.py workbuddy-<uid>.json --yes` |
| 只领奖 | `... --yes --only-claim` |
| 开学季盘点 | `python3 scripts/school_open_day_2026.py ALL --list` |
| 开学季执行 | `python3 scripts/school_open_day_2026.py ALL --run --yes` |
| 抽奖 | `python3 scripts/school_open_day_2026.py ALL --lottery-only --yes` |

账号用**完整 auths 文件名**（`workbuddy-<uid>.json`）而不是 uid 前缀：`load_auth()` 对含路径分隔
或 `.json` 结尾的入参按文件名精确匹配，避免 uid 前缀撞号（`abc123` 命中 `abc1234`）。

### 4.3 子进程执行环境（三条硬要求）

```python
env = os.environ | {
    "PYTHONUNBUFFERED": "1",     # 必须：stdout 被管道接走时 Python 会块缓冲，面板会一直看不到输出
    "PYTHONIOENCODING": "utf-8", # 服务器 LANG 可能是 C，避免非 ASCII 打印炸掉
    "WB2A_AUTHS": <面板解析到的 auth_dir>,   # 面板若用 --auth-dir 覆盖，脚本按同一处找凭据
}
cwd = bin_dir                    # /opt/wb2a/app：scripts/ 与 auths/ 的相对根
stdout = PIPE, stderr = STDOUT   # 合并：脚本的 ERR:/[warn] 也带进度信息，且避免 stderr 管道塞满死锁
```

> `PYTHONUNBUFFERED` 是这套方案里最容易踩的坑：不设的话脚本照常跑完，但面板在整个运行期间
> **一行输出都收不到**，界面看起来像卡死 —— 而进程其实是正常的。

输出**按 bytes 读、`errors="replace"` 解码**，与既有 `run_tool()` 同口径（签到输出含非 UTF-8
字节，`text=True` 会直接抛 `UnicodeDecodeError`）。

## 五、日志解析（界面进度的来源）

脚本没有结构化输出，界面进度只能从日志行解析。这是本设计**最需要测试覆盖**的部分 ——
脚本随上游演进会改文案，解析失败必须**降级成「显示原始日志」而不是显示错误状态**。

### 5.1 识别规则

| 行形态 | 归入 |
|---|---|
| `== <uid8> (<昵称>) ==` | 账号表头：建立 uid8 → 昵称 映射 |
| `[task_runner] <uid8> <code>: …` | 任务项，key = `(uid8, code)` |
| `[school2026] <uid8> <code>: …` | 同上（同一解析器，前缀可配） |
| `[task_runner] <uid8> query …` / `[school2026] <uid8> lottery/draw …` | 账号级事件（无 code） |
| `task_runner done: k=v …` / `school2026 done: k=v …` | 汇总行 |
| `[skip] <uid8> global realm 不适用 CN 任务` | 账号级 skip |
| `ERR: …` | 账号级错误 |

### 5.2 状态分类（同一 item 后写覆盖先写）

| 状态 | 触发文案 | 界面 |
|---|---|---|
| `done` | `claim 200 ok(...)`、`-> 1/1（点亮）`、`-> claimed（本轮已入账）` | ✅ 已完成 |
| `already` | `已领，跳过`、`已完成/已领`、`already_claimed` | ✓ 已领 |
| `planned` | `dry-run 跳过`、`dry-run 不写` | ◇ 待执行（扫描态） |
| `running` | `report i/n …`、`viewed 激活…` | ◐ 进行中 |
| `pending` | `未达 target…WARN 待下次`、`非夜猫窗口…skip pending`、`accept 未登记生效`、`only_claim 跳过` | ○ 待下次 |
| `skip` | `不可伪造(...)`、`任务不存在`、`非映射任务`、`school 未映射(人工/未知)` | — 跳过 |
| `error` | `-> ERR`、`claim 失败`、`report 失败` | ✗ 失败 |

奖励从 `credit=+N` / `energy=+N` 提取，累加到 item 与作业汇总。

### 5.3 汇总行

```
task_runner done: accounts=3 total=57 ok=20 already=30 skipped=5 pending=1 fail=1 credit=+1200 energy=+40
school2026 done: accounts=3 ok=8 already=2 skipped=1 pending=0 fail=0
```

两者都解析成 `summary {accounts,total,ok,already,skipped,pending,fail,credit,energy}`，界面顶部
五个数字卡（已完成 / 已领 / 待下次 / 失败 / 入账积分）。

## 六、界面

任务中心作为 wb2a 页里一张**可折叠卡片**（与「模型目录」「请求统计」同一个 `toggle` 机制），
不新开标签页 —— 它属于 wb2a 网关，不该占一个顶层页。

```
┌ 任务中心 ─────────────────────────────── [扫描待办] [一键完成全部] [只领奖] ┐
│ 成长任务：18 个官方任务，其中 17 个可纯 API 完成；剩余 1 个需真实捐款，脚本自动跳过。│
│ 运行中：一键完成全部 · 1 分 23 秒 · [取消]                                  │
│ ▓▓▓▓▓▓▓▓░░░░░░░░  12/57                                                  │
│ ┌ 已完成 12  已领 30  待下次 3  失败 1  入账 +1200 积分 +40 能量 ┐          │
│                                                                          │
│ 00e26541（昵称 A）                                              3/19      │
│   ✅ create_canvas   1/1   （点亮）                                        │
│   ✓  chat_5          5/5   已领                                           │
│   ◐  expert_5        2/5   真实专家召唤+使用链 ×5                          │
│   ○  black_cat       1/3   非夜猫窗口(23-08 CST)，skip pending             │
│   —  Expert_Philanthropy  不可伪造（真实捐款动作）                          │
│                                                                          │
│ ▸ 原始日志（可展开，含通道过滤）                                            │
└──────────────────────────────────────────────────────────────────────────┘
```

- 按钮的可用性走既有**能力探测**（`can_tasks`：scripts/task_runner.py 存在 + python3 可用），
  检测不到就禁用并给出替代方案（直接在网关侧跑命令），与 `can_login` / `can_checkin` 同构。
- 写操作按钮点击前 `confirm()` 说明「会向腾讯发出真实上报请求」。
- 扫描/运行期间每 2 秒轮询 `/api/tasks/status` + `/api/tasks/log?since=N`，只追加新行，不重渲染整页。
- 运行结束后刷新账号卡片（积分可能变了）。

## 七、风险与边界

| 风险 | 现状 | 处置 |
|---|---|---|
| **与网关排程撞车** | 网关 12:00 跑 school、01:00 跑 cat，用的是同一批脚本；与面板作业并发会重复上报、抢领奖 | 面板侧 flock 互斥 + 启动时排程窗口提示；**彻底的跨进程互斥需要改网关**（让 Go scheduler 也持同一把锁），列为后续 |
| 解析随上游文案漂移 | 脚本是 Sliverkiss 在活跃维护的 | 解析失败降级为「显示原始日志」，不伪造状态；解析规则集中在一处并配单测 |
| 误触发真实写操作 | `--yes` 才写；`scan` 全程只读 | 写操作二次确认；「只领奖」标为最安全的写操作（幂等）；不提供无确认的一键全量 |
| 取消后留下半完成状态 | 脚本中断可能点亮未领奖 | claim 幂等 —— 重跑或「只领奖」即可补；界面在取消后显著提示这一点 |
| 面板未同机部署 | 调不到脚本 | `can_tasks=false`，按钮禁用并说明原因（既有降级模式） |
| 长作业把面板拖死 | 输出量大 | 环形缓冲封顶（2000 行）、items 表封顶、轮询只取增量 |

**明确不做**：不移植 Go 任务引擎、不在面板里存凭据、不提供排程开关的在线编辑（网关无配置
热加载端点，改配置要重启 —— 只读展示时段）。

## 八、改动清单

| 文件 | 改动 |
|---|---|
| `panel.py` | `Config.can_tasks` / `resolved_script_dir` / `python_exe`；`TaskProgress`（解析器）＋ `TaskJob`（作业）＋ `Panel.task_start/task_status/task_cancel/task_capability`；4 个端点；启动横幅与能力透出 |
| `index.html` | 任务中心卡片 + 按钮组 + items 表格 + 原始日志折叠区 + 轮询 JS |
| `tests/test_tasks.py` | 解析器单测（各状态文案、汇总行、奖励提取、未知行降级）、命令构造、能力探测、进程执行与取消、flock 互斥 |
| `tests/test_smoke.py` | 端到端：真起进程走 HTTP 通路（`TestTaskSmoke`） |
| `tests/stub_tasks.py` | 新增：假 task_runner / school 脚本，按真实文案吐日志，支持 `--yes`/`--only-claim` 分支，不发网络请求 |
| `README.md` / `CHANGELOG.md` | 功能表、参数、版本 0.4.0 |

## 九、实现期修正（与本文初稿的差异）

实施时发现三处设计需要修正，都记在这里，避免后来人照着初稿理解代码：

1. **状态收尾顺序** —— 初稿让作业线程在 finally 里释放锁、随后落终态，实际存在
   「界面以为结束了、锁还没放」的窗口（用 0.3s 轮询的 reaper 线程补，仍有竞态）。
   改为**先释放锁与回调、再落终态**，使 `running=False` 严格蕴含锁已释放；
   同时 `running` 只认「进程真的退出」才翻假（取消时置中间态会让调用方误判可重入）。

2. **汇总口径** —— 界面顶部数字初稿按 item 表统计，实测偏小（item 只含脚本打印
   过日志的任务）。改为**优先采信脚本自己的汇总行**（`total/ok/already/pending/fail`），
   只有在汇总行还没出现时才回落到 item 统计。

3. **能力标记的调用点** —— 初稿在 `loadTasks` 首轮顺带读一次 `/api/endpoint`，
   导致「未启用」标签永不出现。改为挂在 `loadEndpoint` 里（与 login/checkin/trial
   的禁用同一处），每次刷新都生效。

另有两处顺带修掉的既有缺陷：

- `Panel.account_op` 的 `enable` 分支 `UnboundLocalError`（重构时删掉了
  `payload = None` 初始化，`enable` 走到 `http()` 就崩，HTTP 侧表现为连接被断开）。
- `TestSmoke` 冒烟测试会向上探测到开发机真实的 `wb2a/app`（带 linux 二进制），
  能力探测结果随机器而变；现显式指向不存在的网关配置，锁定「无 CLI 工具时的
  降级形态」。

## 十、验收

1. `python3 -m unittest discover -s tests -v` 全绿（148 项）。
2. 扫描（dry-run）不产生任何写请求：单测与冒烟各有一项断言 argv 里没有 `--yes`。
3. 一键完成启动后 HTTP 立即返回；轮询能看到逐项状态与汇总数字，且与 stub 脚本
   输出一致（含多账号分组、奖励累加、账号级事件行）。
4. 重复点击被 409 拒绝；取消能在数秒内结束进程并置 `cancelled`。
5. 未部署脚本的环境：`can_tasks=false`，端点给出可读错误而非 500，页面按钮禁用。
6. 真实 `task_runner.py` 对接已验证：命令构造、cwd、环境变量、汇总行解析、
   401 失败的归属与计数均正确（本机 auths 为空，无凭据时不发任何写请求）。

